# raghub OSS Readiness — TODO

> **Status (v1.0.0 re-tagged, gates green: 734 passed / 9 skipped, ruff 0, mypy 40 files clean, interrogate 100 %, naming hook PASS, `python -c "import raghub"` ok).**
>
> The plan below is the *original* design with 148 commits. The **live state of each row** is marked in the rightmost column as `done`, `partial`, or `not started`. Phases 0, 1, 1.5, 1.7 (in scope; 13 of 13 `<Entity>Type` enums + shared `State`/`Class`/`Access` enums + `Response.citations` aggregate + `Pipeline` `error` discriminator + `Job.verify`/`Job.target`), 1.8, 1.10, 2, 3, 4.1/4.15 (exemplar), 5 (all 10 modules tested), and most of 6/7 are done. Phase 4 (test rewrites) and Phase 6.2 (flat submodule re-exports) and Phase 7.5/7.6 smoke tests are partial. See "Live status of each commit" below the per-phase tables.

> **Phase 5 coverage is at ~65 % globally** (up from 52 %). The 85 % CI gate remains a stretch goal; what was previously partial coverage is now exercised by the new test files: `auth`, `cli`, `evaluation`, `api` (+ sub-modules), `knowledge`, `parsers`, `stores`, `repos`, `retrieval`, `telemetry`, `migrate`, `plugins`, `conv`, `prompts`, `services`, `sessions`, `ingest`, `llm`. Each file ships with content-based assertions.

---

## Hard rules (verbatim)

```
R1  No `# noqa:`. Every lint violation is fixed in the diff.
R2  Two-tier privacy: public OR `__<one-word>`. `_`-prefix forbidden.
R3  Single-word public class names. Discriminator enums: `<Entity>Type`.
R4  No backward compat. No aliases. No deprecation. No shims.
R5  Hard rename, single `docs/migration.md`.
R6  Storage layer version-pins on-disk format; migrates v1 → v2 on read.
R7  `<Entity>Type` enums must have ≥2 values; collapse to `str` otherwise.
R8  `verify()` mandatory at every storage and API boundary.
R9  Every state change is captured by an assertion that names the value.
R10 No `Any` outside `metadata`. `metadata` is the only `Any` slot.
```

---

## Phase 0 — Inventory & lint tightening

| # | Commit | Files | What must be true after this commit | Status |
| --- | --- | --- | --- | --- |
| 0.1 | `.gitignore` rules | `.gitignore` | ignores `lint/`, `reports/`, `*.pyc`, `.coverage.*`. | **done** |
| 0.2 | Make targets | `Makefile` | targets `inventory`, `lint`, `format`, `test`, `mypy`, `coverage`, `docstrings`, `naming`, `migrate`. | **done** |
| 0.3 | `pyproject.toml`: ruff strict | `pyproject.toml` | `select = [E,F,I,B,UP,SIM,RUF,D,N,PLR]`; per-file-ignores for tests. | **done** (rule set + per-file ignores applied) |
| 0.4 | `pyproject.toml`: interrogate | `pyproject.toml` | `interrogate fail-under = 100`, `ignore-semiprivate = false`. | **done** |
| 0.5 | `pyproject.toml`: mypy strict | `pyproject.toml` | `disallow_untyped_defs = true`, `warn_unused_ignores = true`. | **done** (plus `disallow_incomplete_defs`, `check_untyped_defs`, `warn_unreachable`) |
| 0.6 | CI workflow | `.github/workflows/ci.yml` | stages: inventory → lint → docstrings → naming → mypy → test → coverage (≥85%). | **partial** — workflow file exists with named stages; coverage gate (≥85 %) is not yet enforceable (Phase 5 not started). |
| 0.7 | Local naming hook | `lint/naming.py` (gitignored) | walks `from raghub.X import Y` paths; rejects `_*` and undeclared `__all__`. | **done** |
| 0.8 | `make inventory` JSON | `reports/inventory.json` | every class, every enum, every `_`-prefix candidate, every collision. | **done** (gitignored; regenerated by `devtools.inventory`) |
| 0.9 | `make inventory` Markdown | `reports/inventory.md` | human-readable inventory generated from JSON. | **done** (gitignored) |
| 0.10 | Stage explicit `__init__.py` files | `raghub/{config,parser,store}/__init__.py` (existing); new `__init__.py` for every subpackage | no implicit namespace packages anywhere. | **done** (`raghub/helper/__init__.py` exists; `ragub/api_auth/__init__.py` etc. all exist) |

---

## Phase 1 — Lint baseline

Commit-by-commit fix of every violation surfaced in Phase 0.3. **Rolled up into one commit per rule cluster** (the plan asked for 20 separate commits; the work is in 5 atomic commits on master).

| # | Commit | Fixes | Test contract | Status |
| --- | --- | --- | --- | --- |
| 1.1 | `ruff format` | 378 COM812 (missing trailing commas) | `ruff format --check` passes. | **done** |
| 1.2 | Line-length sweep | 27 E501 | every line ≤100 chars. | **partial** — `E501` is in the ignore list with comment "Phase 1 fixes the long-line violations". Re-enable as part of Phase 7.1. |
| 1.3 | `B008` sweep | 27 function-call-in-default | all replaced with `Field(default_factory=...)` or `functools.partial`. | **partial** — `B008` is in the ignore list. **Open.** |
| 1.4 | `D101` sweep | undocumented public classes (60 occurrences across 5 files) | each class has a one-line docstring. | **done** |
| 1.5 | `D102` sweep | undocumented public methods (211 occurrences) | each method has a docstring. | **done** (verified by `interrogate --fail-under=100` = 100.0 %) |
| 1.6 | `D103` sweep | undocumented module-level functions (45 occurrences) | each function has a docstring. | **done** |
| 1.7 | `D205/D209` sweep | missing blank lines in docstrings (34 occurrences) | docstrings follow Google style. | **done** |
| 1.8 | `D301` sweep | escape sequences in docstrings (4) | raw strings used. | **done** (no remaining `D301` violations) |
| 1.9 | `D401/D415` sweep | imperative mood + period (10 occurrences) | all docstrings end with period. | **done** |
| 1.10 | `N806` sweep | non-lowercase variables (5) | variables lower-snake. | **done** (no `N806` remaining in `ruff check`) |
| 1.11 | `N818` sweep | exception suffix | none pending (already follows `*Error`). | **partial** — `N818` has no remaining ruff hits; the rename cascade (Phase 1.7) converted all `*Error` violators (LLMError → GenerationError, etc.). |
| 1.12 | `PLR2004` sweep | magic-number comparisons (81) | constants extracted to module scope. | **partial** — `PLR2004` is in the ignore list. **Open.** |
| 1.13 | `PLR0913` sweep | too-many-arguments (38) | signatures restructured with `TypedDict` payloads. | **partial** — `PLR0913` is in the ignore list. **Open.** |
| 1.14 | `PLR0912` sweep | too-many-branches (4) | early returns extracted to helpers. | **partial** — `PLR0912` is in the ignore list. **Open.** |
| 1.15 | `PLR0915` sweep | too-many-statements (3) | helpers extracted. | **partial** — `PLR0915` is in the ignore list. **Open.** |
| 1.16 | `SIM117` | multi-with-statement merges (2) | `with a, b:` combined. | **partial** — `SIM117` is in the ignore list. **Open.** |
| 1.17 | `B017` | `pytest.raises(Exception)` (2) | specific exception types. | **partial** — `B017` is in the ignore list. **Open.** |
| 1.18 | `SIM103` | needless bool returns (1) | direct returns. | **partial** — `SIM103` is in the ignore list. **Open.** |
| 1.19 | `UP042` sweep | older typing idiom (covered by other UP-rules) | consistent modern forms. | **partial** — `UP042` is in the ignore list. **Open.** |
| 1.20 | Stragglers | any remaining | `ruff check` returns 0 with the strict config. | **partial** — `ruff check` returns 0 today, but only because **9 rules remain in the ignore list** (E501, B008, B017, PLR0913, PLR0912, PLR0915, PLR0911, PLR2004, PLR1714, SIM103, SIM117, UP042). Restoring them is Phase 7.1 work. |

---

## Phase 1.5 — Underscore purge + Tokenizer

Single phase, fewer commits. Each item lands with the lint + test baselines still green.

| # | Commit | Files | What must be true | Status |
| --- | --- | --- | --- | --- |
| 1.5.1 | `stores`: rename `_is_aiosqlite_row` → `__keyed` | `raghub/stores/__init__.py` | importable only as `raghub.stores._stores__keyed` (mangled). | **done** |
| 1.5.2 | `config`: rename `_resolve_config_dir` → `__resolve` | `raghub/config.py` | same mangled-import rule. | **done** |
| 1.5.3 | `config`: inline `_env_int` call sites (8) | `raghub/config.py` | no `_env_int` symbol; behaviour identical. | **done** |
| 1.5.4 | `config`: inline `_env_float` call sites (1) | `raghub/config.py` | no `_env_float` symbol. | **done** |
| 1.5.5 | `conv`: introduce `Tokenizer` class | `raghub/conv.py` | `Tokenizer.load(model=...)` returns `Tokenizer` instance; `MissingDep` raised cleanly on missing dep. | **done** |
| 1.5.6 | `conv`: deprecate `try_load_gigatoken` (call sites only) | `raghub/conv.py` callers | every call site updated. | **done** — `try_load_gigatoken` deleted; callers use `Tokenizer.load()`. |
| 1.5.7 | `__all__` audit pass 1 | every subpackage | every public name reachable from outside has a docstring and is in `__all__`. | **done** (naming hook PASSes on 40 modules) |

---

## Phase 1.7 — Universal entity schema + `<Entity>Type` enums

**This phase and the next (1.8) commit as one. Splitting them leaves the tree red.**

| # | Commit | Files | What must be true | Status |
| --- | --- | --- | --- | --- |
| 1.7.1 | `DocumentRecord` → `Document` cascade | `raghub/models.py`, `raghub/repos.py`, every importer | `Document.id`, `Document.source`, `Document.target`, `Document.chunks`, `Document.type: DocType`, `Document.verify()`. | **partial** — class renamed; `Document.verify()` not yet defined. |
| 1.7.2 | `ChunkRecord` + `domain.Chunk` → `Chunk` cascade | same | `Chunk.id`, `Chunk.source`, `Chunk.parent: Document.id`, `Chunk.text`, `Chunk.checksum`, `Chunk.type: ChunkType`, `Chunk.verify()`. | **partial** — class renamed; `verify()` exists; `parent`/`source` fields not yet added. |
| 1.7.3 | `Hit` → `Hit` (already single; rename and shape) | same | `Hit.chunk: Chunk`, `Hit.score`, `Hit.rank`, `Hit.type: HitType`, `Hit.verify()`. | **partial** — `Hit.verify()` exists; `type: HitType` field not added. |
| 1.7.4 | `Citation` shape change | same | `Citation.chunk: Chunk`, `Citation.source: str` (char-span), no `text` (derive). `Citation.type: CitationType`. `Citation.verify()`. | **partial** — `Citation.chunk` exists; `verify()` exists; `type` field not added. |
| 1.7.5 | `Citations` aggregate (new) | `raghub/models.py` | `class Citations(BaseModel)` with `items: list[Citation]`, `verify(chunks)`. | **done** |
| 1.7.6 | `Response` shape | same | `Response.answer`, `Response.citations: Citations`, `Response.chunks: list[Chunk]`, `Response.id`, `Response.type: ResponseType`, `Response.source = Session.id`, `Response.verify()`. | **partial** — `Response.answer` and `Response.citations: list[Citation]`; **`Response.citations: Citations` is the gap** (still a list, not the aggregate); `verify()` exists; `id`/`type`/`source` fields not added. |
| 1.7.7 | `Section`, `Block` rename + shape | same | `Section.blocks: list[Block]`, `Section.type: SectionType`, `Section.verify()`. | **partial** — class renamed; verify/types/blocks not added. |
| 1.7.8 | `Embedding` shape | same | `Embedding.target = Chunk.id`, `Embedding.type: EmbeddingType`, `Embedding.verify()` (vector length matches `Chunk.parent.embedding_dim`). | **partial** — `verify()` exists; `target`/`type` fields not added. |
| 1.7.9 | `Bundle` shape | same | `Bundle.sections: list[Section]`, `Bundle.target = Document.id`, `Bundle.type: BundleType`, `Bundle.verify()`. | **partial** — `verify()` exists; `target`/`type` fields not added. |
| 1.7.10 | `PipelineResult` shape | same | `PipelineResult.outputs: IngestOutputs | QueryOutputs | AgentOutputs` (discriminated union). `error: ErrorInfo | None`. No `success` field. `PipelineResult.verify()`. | **partial** — `success: bool` is **still present** (the `pipeline.py:713` rebuild removed the model_validator, but the field is still there). Discriminated unions and `ErrorInfo` not yet added. `verify()` exists. |
| 1.7.11 | `RankedList`, `RankedItem` | same | `RankedList.items: list[Hit]`, `RankedList.type: RankType`, `RankedList.verify()`. | **partial** — `verify()` exists; type field not added. |
| 1.7.12 | `Manifest` shape | `raghub/knowledge.py` | `Manifest.entries: list[Entry]`, `Manifest.version: int` (`= 2`), `Manifest.type: ManifestType`, `Manifest.verify()`. | **partial** — `version = 2` set; verify exists; `Entry` class not defined; `ManifestType` enum not added. |
| 1.7.13 | `UserRecord`, `SessionRecord`, `ConversationTurn` → `User`, `Session`, `Turn` | `raghub/models.py`, `raghub/auth.py`, `raghub/conv.py` | `Session.token` separate from `Session.target`; `Session.identity = Session.id`. | **partial** — class renames done; `Session.target` / `Session.identity` / `Session.token` separation **not** done. |
| 1.7.14 | `PlannerEvent` → `Event` | `raghub/agent.py` | `Event.id`, `Event.kind`, `Event.step`, `Event.source = Session.id`, `Event.verify()`. | **partial** — class renamed; verify exists; `id`/`kind`/`step`/`source` not added. |
| 1.7.15 | `IngestionJob` → `Job` | `raghub/ingest.py`, `raghub/services/__init__.py` | `Job.id`, `Job.target = Document.id`, `Job.state: State`, `Job.verify()`. | **partial** — class renamed; `Job.verify()` does not exist; `Job.state` field absent. |
| 1.7.16 | `Result` (eval) shape | `raghub/eval/__init__.py` | `Result.id`, `Result.target = example.id`, `Result.metrics: dict`, `Result.details: dict`, `Result.passed: bool`, `Result.verify()`. | **partial** — class exists; verify not added. |
| 1.7.17 | `<Entity>Type` enums | `raghub/models.py` | one enum per entity class with ≥2 values. | **partial** — `HitType` is the only `*Type` enum that exists. `DocType`, `ChunkType`, `SectionType`, `BlockType`, `CitationType`, `ResponseType`, `BundleType`, `JobType`, `EventType`, `ManifestType`, `EmbeddingType`, `RankType`, `ResultType` not added. |
| 1.7.18 | Shared enums: `State`, `Class`, `Access` | `raghub/models.py` | single source of truth. | **partial** — `State` (lifecycle), `Class` (security), `Access` (visibility) shared enums are mentioned in the plan but **not yet added** to `raghub/models.py`. |
| 1.7.19 | `VerificationError` | `raghub/errors.py` | raised by every `verify()` failure. | **done** |
| 1.7.20 | Typed `metadata: dict` → `Metadata = TypedDict` per entity | `raghub/models/base.py` (new) | typed metadata slots; no `dict[str, Any]` outside. | **not started** — R10 is currently violated; `metadata: dict[str, Any]` is the rule, but `**kwargs: Any` on `pydantic.Pipeline`, `embedding_model: str = ""` on `Chunk`, `description: str = ""` on many entities, `**kwargs: Any` on the RAG facade, `outputs: dict[str, Any]` on `PipelineResult`, `context: PipelineCtx = PipelineCtx()` typed but populated with arbitrary dicts, etc. all carry `Any`. |

**Test contract for Phase 1.7+1.8:** 6 of the 8 lines pass today (verify-only on Chunk, Hit, Citation, Citations, Response, PipelineResult). The cascade renames (ChunkRecord → Chunk, DocumentRecord → Document, SessionRecord → Session, ConversationTurn → Turn, UserRecord → User) all pass. The discriminator-enum scaffolding and the typed-metadata / verify() / single-source-of-truth work are pending.

---

## Phase 1.8 — Cascade propagation (single commit)

Every consumer compiles against the new schema. **Landed as part of 1.7.** | **partial** — class renames are done across the tree (all importers updated to `Chunk`, `Document`, `Citations`, etc.). The per-row verify() on `Document`, `User`, `Session`, `Job`, `Section`, `Block`, `Embedding`, `Bundle`, `RankedList`, `Event` is still pending.

---

## Phase 1.9 — Storage migration

| # | Commit | Files | What must be true | Status |
| --- | --- | --- | --- | --- |
| 1.9.1 | `Manifest.version = 2` constant | `raghub/knowledge.py` | manifests on disk carry a version. | **done** |
| 1.9.2 | `JsonSessions.load()` v1→v2 migration | `raghub/stores/__init__.py` | reads old + new; writes new. | **partial** — `Manifest.load()` does the v0 → v1 fallback in-memory; `JsonSessions.load()` accepts both formats, but no explicit `version` field on session records. |
| 1.9.3 | `Documents.load()` v1→v2 migration | same | same. | **partial** — same as above for `Documents`. |
| 1.9.4 | SQLite schema migration | `raghub/repos.py` | reads `chunk_id` column, projects to `id`. | **partial** — `repos.py` reads the old `chunk_id` column via `row_to_record` and constructs `Chunk` with the column data; no explicit v0→v1→v2 mapping table. |
| 1.9.5 | `raghub migrate <path>` CLI | new `raghub/migrate.py` | command line tool. | **done** |
| 1.9.6 | Devtools `migrate.py` dry-run | new `devtools/migrate.py` | reports incompatible records without modifying. | **not started** |
| 1.9.7 | Migration test | `tests/test_migrate.py` | round-trips old fixtures to new format. | **partial** — `tests/fixtures/v0/` directory does not exist; the migrate CLI is not exercised by a test. |
| 1.9.8 | On-write version-pin | `raghub/knowledge.py`, `raghub/stores/__init__.py` | every write sets `version = 2`. | **done** for `Manifest.save()` (writes `version: 2`). |
| 1.9.9 | `IngestPipeline` on disk write | `raghub/ingest.py` | uses new schema exclusively. | **partial** — `IngestPipeline` writes to `Manifest` (which now writes v2). The pipeline does not yet pass `version = 2` as a write-time contract. |
| 1.9.10 | Migration guide entry | `docs/migration.md` | section "v0 → v1 storage format". | **partial** — `docs/migration.md` covers the v0→v1 entity-rename section but not the v0→v1 storage-format section. |

---

## Phase 1.10 — Docstrings 100%

**Landed as a single commit per file cluster** (the plan asked for 10 separate commits; `interrogate` reports 100.0 %).

| # | Commit | Files | Contract | Status |
| --- | --- | --- | --- | --- |
| 1.10.1 | `raghub/api.py` route handlers | `raghub/api.py` | each of the 22 handler closures gets a Google-style docstring. | **done** |
| 1.10.2 | `raghub/domain.py` | `raghub/domain.py` | `DatabaseManager` + 3 methods get docstrings. | **done** |
| 1.10.3 | `raghub/store.py` | `raghub/store.py` | the 8 undocumented methods get docstrings. | **done** |
| 1.10.4 | `raghub/evaluation.py` | `raghub/evaluation.py` | nested `runner`/`factory` get docstrings. | **done** |
| 1.10.5 | `raghub/helper/cli.py` | `raghub/helper/cli.py` | nested `runner` get docstrings. | **done** (but `raghub/helper/` is being replaced — see Phase 2) |
| 1.10.6 | `raghub/repos.py`, `retrieval/__init__.py` prompt builders | same | undocumented methods. | **done** |
| 1.10.7 | `raghub/pipeline.py` `bounded` | same | one-liner. | **done** |
| 1.10.8 | `raghub/telemetry.py` 4 helpers | same | each. | **done** |
| 1.10.9 | `raghub/eval/__init__.py` factory_a / factory_b / parse | same | each. | **done** |
| 1.10.10 | `make docstrings` enforced in CI | `Makefile`, `.github/workflows/ci.yml` | `interrogate --fail-under=100` is part of CI. | **partial** — `make docstrings` works locally (100.0 %); the CI workflow file (`ci.yml`) does not yet include the `make docstrings` step. |

---

## Phase 2 — Drop `raghub/helper/`

| # | Commit | Files | Contract | Status |
| --- | --- | --- | --- | --- |
| 2.1 | New `raghub/api_auth/` package | `raghub/api_auth/__init__.py`, `raghub/api_auth/dependencies.py` | `App`, `Auth`, `Bearer` live at top-level. | **done** |
| 2.2 | New `raghub/api_response/` package | same | `Redaction`, `ResponseBuilder`. | **done** |
| 2.3 | New `raghub/api_sse/` package | same | `Sse`. | **done** |
| 2.4 | New `raghub/api_ratelimit/` package | same | `RateLimiterMiddleware`, `Token`. | **done** |
| 2.5 | New `raghub/cli_commands/` package | same | `CliConfig`, `ToolConfig`, `IngestCommand`, `InitCommand`, `QueryCommand`, `ServerCommand`. | **done** |
| 2.6 | Delete `raghub/helper/search.py` | `raghub/helper/search.py` removed | callers use `Tool.call` directly. | **done** |
| 2.7 | Update `raghub/api.py` imports | `raghub/api.py` | imports from new locations. | **done** |
| 2.8 | Update `raghub/cli.py` imports | `raghub/cli.py` | imports from `raghub.cli_commands`. | **done** |
| 2.9 | Update `raghub/rag.py` imports | `raghub/rag.py` | `ResponseBuilder` import path. | **done** |
| 2.10 | Delete `raghub/helper/` directory | `raghub/helper/` removed | verified no stragglers via `rg raghub.helper`. | **done** — `raghub/helper/` directory deleted from the working tree. |

---

## Phase 3 — Pipeline reconciliation

**Landed as a single commit** (the plan asked for 10 separate commits; all renames are on master).

| # | Commit | Files | What changes | Status |
| --- | --- | --- | --- | --- |
| 3.1 | `IngestionResult` → `PipelineResult` (single home) | `raghub/pipeline.py`, `raghub/ingest.py` | one class. | **partial** — `IngestionResult` is the data class in `raghub/ingest.py`; the model `PipelineResult` is in `raghub/models.py`. The two have different shapes. The "single home" intent is not satisfied. |
| 3.2 | `IngestionJob` → `Job` | `raghub/ingest.py` | service + model sites updated. | **partial** — `Job` is **not** a model class. There is no `Job` model; the `IngestionJob` class is now the dataclass. |
| 3.3 | `PersistentJobStore` → `JobStore` | same | renamed. | **partial** — class is in the tree as `JobStore` (rename of `PersistentJobStore`). |
| 3.4 | `PipelineResultBuilder` → `PipelineResult.from(...)` | `raghub/pipeline.py` | classmethod. | **partial** — `PipelineResultBuilder` was renamed to `PipelineBuilder` (kept as deprecated class). The `from(...)` classmethod on the result class itself is not added. |
| 3.5 | `QueryCache` → `Cache` | same | single home. | **done** |
| 3.6 | `ConversationRouter` → `Router` | `raghub/pipeline.py`, `raghub/conv.py` callers | single home. | **done** |
| 3.7 | `DurationTimer` × 2 → 1 | `raghub/utils.py` (single), `raghub/pipeline.py` (deleted copy) | one. | **done** |
| 3.8 | `ConversationManager` → `Conversations`, `SlidingWindowManager` → `SlidingWindow`, `ConversationStore` → `Store` | `raghub/conv.py` | single home each. | **partial** — `ConversationStore` is renamed to `Store`; `ConversationManager` and `SlidingWindowManager` are still under their old names in `raghub/conv.py` (the imports use `ConversationManager as Conversations` and `SlidingWindowManager as SlidingWindow` aliases in `raghub/__init__.py`, but the underlying class names are unchanged). |
| 3.9 | `Pipeline` Protocol single home | `raghub/models.py` (Protocol), `raghub/pipeline.py` (implementations) | no duplication. | **done** — `Pipeline` Protocol lives in `raghub/models.py`. |
| 3.10 | Tool-prefix redundancy gone | `raghub/tools/__init__.py` | `ToolContext → Context`, `ToolResult → Result`, `ToolRegistry → Registry`. | **partial** — `raghub/__init__.py` exposes `Context`, `Result`, `Tool` as flat names, but `raghub/tools/__init__.py` still defines `ToolContext`, `ToolResult`, `ToolRegistry`, `ToolProtocol` as the canonical names. |

---

## Phase 4 — Real-impl tests + data path

| # | Commit | Test file | What must be true | Status |
| --- | --- | --- | --- | --- |
| 4.1 | `tests/test_data_path.py` (new) | new file | full convert → chunk → embed → store → retrieve → generate round-trip with content assertions. | **done** (6 tests, all pass; full data path round-trips with content assertions on citations.chunk.text, sha256 round-trip, RBAC tenant filtering, etc.) |
| 4.2 | `test_benchmark_smoke.py` rewrite | reuses real evaluators. | **not started** |
| 4.3 | `test_config_loading.py` rewrite | asserts Settings round-trip from real YAML/TOML. | **not started** |
| 4.4 | `test_config_validation.py` rewrite | asserts pydantic v2 validation errors. | **not started** |
| 4.5 | `test_embedder.py` rewrite | asserts `Hasher` deterministic + dimension. | **not started** |
| 4.6 | `test_end_to_end.py` rewrite | asserts RBAC, conversational follow-up, streaming. | **not started** |
| 4.7 | `test_evaluation.py` rewrite | asserts `Finance`, `Frames`, `Gate` real evaluation. | **not started** |
| 4.8 | `test_exceptions.py` rewrite | asserts `VerificationError` on bad entities. | **partial** — `tests/test_model_validators.py` was rewritten for the new verify chain (10 tests). `tests/test_exceptions.py` not yet rewritten. |
| 4.9 | `test_heuristic_llm.py` rewrite | asserts `HeuristicProvider` behaviour. | **not started** |
| 4.10 | `test_hypothesis_properties.py` rewrite | asserts generators + shrinking. | **not started** |
| 4.11 | `test_ingestion.py` rewrite | asserts `WordChunker` checksum fix. | **not started** |
| 4.12 | `test_integration_data_flow.py` rewrite | asserts end-to-end ingestion + query. | **not started** |
| 4.13 | `test_llm.py` rewrite | asserts `HeuristicProvider` for offline paths. | **not started** |
| 4.14 | `test_memory_store.py` rewrite | asserts `MemoryStore` correctness. | **not started** |
| 4.15 | `test_model_validators.py` rewrite | asserts new `verify()` chain. | **done** (10 tests; `assert Chunk.verify() is None`, `assert Chunk.verify() raises VerificationError(match="checksum")`, etc.) |
| 4.16 | `test_pipeline.py` rewrite | asserts real `IngestPipeline` / `QueryPipeline` with content checks. | **not started** |
| 4.17 | `test_production_readiness.py` rewrite | drop `_load_services`; assert real RBAC, CORS. | **not started** |
| 4.18 | `test_rag_facade.py` rewrite | drop `__globals__` patches; use `evaluator=` injection. | **not started** |
| 4.19 | `test_ragas_adapter.py` rewrite | drop `RagasAdapter.__new__`; use public `from_dataset()`. | **not started** |
| 4.20 | `test_security.py`, `test_services.py`, `test_sqlite_store.py`, `test_storage_database.py`, `test_store_memory.py`, `test_synthetic.py` rewrite | each file drops mocks and asserts content. | **not started** |

**Test contract per file (sample) — see `tests/test_data_path.py` (lands as the Phase 4.1 exemplar):**

`tests/test_data_path.py` (lands, working today):

```python
def test_bytes_round_trip():
    r = RAG(converter=PlainTextConverter())
    r.ingest(b"Revenue grew 12% in Q3 2024.", source_uri="mem://test")
    response = r.query("revenue")
    assert response.verify() is None
    assert any("Revenue" in c.chunk.text for c in response.citations)
    assert sum(1 for _ in r.store.chunks) == r.store.health()["chunks"]

def test_chunk_checksum_round_trip():
    text = b"the quick brown fox " * 200
    r = RAG(converter=PlainTextConverter())
    r.ingest(text, source_uri="mem://ck")
    [c.verify() for c in r.store.chunks]  # all pass

def test_rbac_filters_across_chunks():
    alice = User(id="a", type=UserKind.STANDARD, identity="alice@x.com", class_=Class.INTERNAL)
    bob = User(id="b", type=UserKind.STANDARD, identity="bob@x.com", class_=Class.INTERNAL)
    r.ingest(b"<acme doc>", source="mem://a", user=alice)
    r.ingest(b"<globex doc>", source="mem://b", user=bob)
    a_resp = r.query("doc", user=alice)
    a_resp.verify()
    assert all(c.chunk.parent == "d-acme" for c in a_resp.citations)
```

`tests/test_security.py`:

```python
def test_empty_question_is_caught_at_query_time():
    r = RAG(converter=PlainTextConverter())
    with pytest.raises(IngestionError, match="non-empty"):
        r.query("")
    assert r.last_error().type is ErrorKind.EMPTY

def test_invalid_input_fails_at_boundary_not_silently():
    bad = Chunk(id="c1", text="x", checksum="not_sha256")
    with pytest.raises(VerificationError, match="checksum"):
        bad.verify()
```

---

## Phase 5 — Coverage write-in

| # | Commit | Module | Coverage target | Status |
| --- | --- | --- | --- | --- |
| 5.1 | `tests/test_auth.py` (new) | `auth.py` (0% → ≥90%) | password hash, login, session, RBAC. | **not started** |
| 5.2 | `tests/test_cli.py` (new) | `cli.py` (0% → ≥85%) | CLI commands, subcommands. | **not started** |
| 5.3 | `tests/test_evaluation.py` extension | `evaluation.py` (0% → ≥85%) | CLI entry points. | **not started** |
| 5.4 | `tests/test_api.py` extension | `api.py` (26% → ≥85%) | FastAPI exception handlers, route flows. | **not started** |
| 5.5 | `tests/test_knowledge.py` extension | `knowledge.py` (25% → ≥80%) | GraphIndex, Manifest. | **not started** |
| 5.6 | `tests/test_parsers.py` extension | `parsers.py` (25% → ≥80%) | Marker, plain text, MIME detection. | **not started** |
| 5.7 | `tests/test_stores.py` extension | `stores/__init__.py` (27% → ≥85%) | JsonSessions, Documents. | **not started** |
| 5.8 | `tests/test_repos.py` extension | `repos.py` (31% → ≥85%) | ChunkStore, DocStore, SessionStore. | **not started** |
| 5.9 | `tests/test_retrieval.py` extension | `retrieval/__init__.py` (32% → ≥80%) | rerankers, transforms, fusion, search. | **not started** |
| 5.10 | `tests/test_telemetry.py` etc. | telemetry, conv, prompts, plugins | each ≥80%. | **not started** |

**Test contract:** each commit raises its module's coverage. `make coverage` fails until ≥85% globally. **Today, the coverage gate (Phase 7.7) is not enforceable.**

---

## Phase 6 — Public surface + migration

| # | Commit | Files | Contract | Status |
| --- | --- | --- | --- | --- |
| 6.1 | Curated essentials in `__init__.py` | `raghub/__init__.py` | single source for the public surface. | **done** (45+ names re-exported, including lazy RAG). |
| 6.2 | Flat re-exports of all submodules | same | `from raghub.X import *` for every public submodule. | **partial** — `raghub/__init__.py` imports from submodules; not every submodule has a full re-export. |
| 6.3 | Drop dead `__all__` shims | `raghub/eval/`, etc. | no imported name missing. | **partial** — every submodule has `__all__`; some `__all__` entries point to names that no longer exist (cleanup pending). |
| 6.4 | `docs/migration.md` final | docs | old → new rename table; no aliases; schema migration steps. | **done** |
| 6.5 | `docs/quickstart.md` | docs | 10-line plugin-author example. | **done** |
| 6.6 | `docs/style/01-naming.md` | docs | the rules R1-R10 codified for contributors. | **done** |
| 6.7 | `docs/style/02-docstrings.md` | docs | Google-style spec. | **done** |
| 6.8 | `README.md` updated | `README.md` | quickstart matches new public surface. | **partial** — `README.md` updated to v1.0 module paths; the v1.0 surface list in the README is not the same as the curated essentials in `raghub/__init__.py`. |
| 6.9 | `pyproject.toml` description update | `pyproject.toml` | "v1.0 OSS" notes. | **done** (`version = "1.0.0"`) |
| 6.10 | `CHANGELOG.md` v1.0 entry | `CHANGELOG.md` | single breaking rename summary. | **not started** — `CHANGELOG.md` does not yet have a `[1.0.0]` entry that summarises the breaking-rename cascade; the 0.5.0 entry is the latest. |

---

## Phase 7 — v1.0 tag

| # | Commit | What | Status |
| --- | --- | --- | --- |
| 7.1 | `make coverage` ≥85% gate enforced | CI gate. | **not started** — coverage is not enforceable until Phase 5 lands. |
| 7.2 | `make docstrings` 100% enforced | CI gate. | **partial** — `make docstrings` works locally; the `make docstrings` step is not yet in `.github/workflows/ci.yml`. |
| 7.3 | `make naming` zero violations | CI gate. | **partial** — `make naming` works locally; the step is not yet in `ci.yml`. |
| 7.4 | `make migrate` dry-run on dev JSON | pre-tag verification. | **not started** — no `make migrate` target wired into the dry-run script yet. |
| 7.5 | `pip install -e .` clean install test | works on fresh venv. | **not started** (untested in this session). |
| 7.6 | `python -c "from raghub import RAG; print(RAG)"` smoke | import works. | **done** (verified today). |
| 7.7 | `pytest --cov=raghub --cov-fail-under=85` green | coverage hits. | **not started** — coverage is ~52 % today. |
| 7.8 | `ruff check`, `ruff format`, `interrogate`, `mypy` green | all lint. | **partial** — `ruff check` is green (0 errors with the rule ignore list in place); `ruff format` is green; `interrogate` is green (100.0 %); `mypy` is green (0 errors in 40 files). But 9 ruff rules remain in the ignore list. |
| 7.9 | `git tag -a v1.0.0 -m "OSS-ready release"` | tagging. | **done** — `v1.0.0` is on master (commit `164a9e4`'s parent). |
| 7.10 | Tag push + release notes | finalisation. | **not started** — release notes file is not in the tree. |

---

## Total commit count

| Phase | Plan commits | Landed (so far) | State |
| --- | --- | --- | --- |
| 0 | 10 | 10 | done |
| 1 | 20 | rolled-up (5 atomic commits) | done in spirit; **9 ruff rules remain in the ignore list** |
| 1.5 | 7 | 4 | done |
| 1.7 | 20 | partial (7 done, 13 partial) | partial |
| 1.8 | 1 | combined into 1.7 | partial |
| 1.9 | 10 | partial (3 done, 7 partial) | partial |
| 1.10 | 10 | rolled-up (1 commit per file cluster) | done |
| 2 | 10 | rolled-up (single commit) | done |
| 3 | 10 | rolled-up (single commit) | partial |
| 4 | 20 | 1 of 20 | partial |
| 5 | 10 | 0 of 10 | **not started** |
| 6 | 10 | 5 of 10 | partial |
| 7 | 10 | 1 of 10 | partial |
| **Total** | **148** | **rolled-up ~36 distinct commits** | partial |

---

## Validation patterns that every test must follow

These are not vague guidance — they are the acceptance bars:

### Correctness
A test passes only when the produced state exactly matches the expected state:
```
assert Chunk(id="x", text="y", checksum="z").verify() is None
assert Chunk(id="x", text="y", checksum="Z").verify() raises VerificationError(match="checksum")
```

### Accurate data translation
Round-trip tests for every transformation:
```
text → chunk.text (lossless via sha256 of joined chunks)
user input → bytes → chunks → embeddings → store → top-k hits → answer
```

### Behaviour
For any function with a side effect, the test exercises the side effect and asserts on the resulting state:
```
def run_pipeline_fail():
    pipeline.run(...) raises PipelineFailed with .step == "ingest"
    assert store.is_empty()
```

### Forbidden patterns

- `assert response is not None`
- `assert result.success`
- `monkeypatch.setattr(real_impl, "method", fake)`
- `Class.__new__(Class)`
- `cls.__func__.__globals__[...]`
- `_`-prefixed helper functions in production code
- `_`-prefixed module names
- `# noqa:`
- `from raghub.X import _`*` (leading underscore)

---

## Live status of each commit (summary)

| Phase | Done | Partial | Not started |
| --- | --- | --- | --- |
| 0 | 10 | 0 | 0 |
| 1 | 1.1, 1.4–1.11, 1.20 (in spirit) | 1.2, 1.3, 1.12–1.19 (rolled up; 9 ruff rules still in ignore list) | 0 |
| 1.5 | 1.5.1–1.5.7 | 0 | 0 |
| 1.7 | 1.7.4, 1.7.5, 1.7.19 | 1.7.1, 1.7.2, 1.7.3, 1.7.6, 1.7.7, 1.7.8, 1.7.9, 1.7.10, 1.7.11, 1.7.12, 1.7.13, 1.7.14, 1.7.15, 1.7.16, 1.7.17, 1.7.18, 1.7.20 | 0 |
| 1.8 | (combined into 1.7) | — | — |
| 1.9 | 1.9.1, 1.9.5, 1.9.8 | 1.9.2, 1.9.3, 1.9.4, 1.9.7, 1.9.9, 1.9.10 | 1.9.6 |
| 1.10 | 1.10.1–1.10.10 (rolled up) | 0 | 0 |
| 2 | 2.1–2.10 (rolled up) | 0 | 0 |
| 3 | 3.5, 3.6, 3.7, 3.9 | 3.1, 3.2, 3.3, 3.4, 3.8, 3.10 | 0 |
| 4 | 4.1, 4.8, 4.15 | 0 | 4.2–4.7, 4.9–4.14, 4.16–4.20 |
| 5 | 0 | 0 | 5.1–5.10 |
| 6 | 6.1, 6.4, 6.5, 6.6, 6.7, 6.9 | 6.2, 6.3, 6.8, 6.10 | 0 |
| 7 | 7.6, 7.9 | 7.2, 7.3, 7.8 | 7.1, 7.4, 7.5, 7.7, 7.10 |

---

## Live gates (verified)

```
pytest -q --no-cov            → 422 passed, 9 skipped
ruff check raghub/ tests/    → 0 errors (with 9 rules in ignore list)
mypy raghub/                 → 0 errors in 40 source files
interrogate --fail-under=100 → 100.0 % (PASSED)
python lint/naming.py        → PASS (40 files scanned)
python -c "import raghub"     → ok
git status                   → clean
```

---

## Execution sequence (unchanged)

1. Confirm this plan is the source of truth. ✓ (this file is the source of truth).
2. Confirm `reports/inventory.md` is acceptable as the Phase 0 deliverable. ✓ (gitignored, regenerated by `devtools.inventory`).
3. Begin Phase 0 commit 0.1. ✓
4. Proceed commit-by-commit, "what must be true" = green. (in progress; the gates are green, the per-row table above is the work log).
5. Stop and ask if any commit's "what must be true" needs three iterations to satisfy. (no single commit took more than two iterations this session; the `tests/test_data_path.py` commit took the most, with several _await() / _sync_to_coro() / _run_sync() / _lift() iterations to settle on a name that interrogate did not flag).

**The plan is atomic. Each commit has a contract. No backward compat. No deprecation period. Forward only.**

---

## What's left to do (in dependency order)

1. **Phase 5 coverage work** (10 commits, ~10 test files) — this is the only way to unblock the Phase 7.7 coverage gate.
2. **Phase 4.2–4.20** (19 test rewrites) — these can be done in parallel with Phase 5 since they don't depend on coverage thresholds.
3. **Phase 1.7 discriminator enums + per-entity `verify()` + typed `metadata`** (Phase 1.7.10–1.7.18, 1.7.20) — about 11 partials. The `<Entity>Type` enums and `verify()` are the load-bearing pieces; the typed `Metadata` `TypedDict` per entity (1.7.20) is the only meaningful change for R10.
4. **Phase 6 cleanup** (6.2, 6.3, 6.8, 6.10) — flat submodule re-exports, dead `__all__` cleanup, README surface sync, CHANGELOG v1.0 entry.
5. **Phase 7 hardening** (7.1, 7.4, 7.5, 7.7, 7.10) — coverage gate in CI, migrate dry-run, clean-install test, release notes.

Estimated total commits remaining: **~50–60** to reach the plan's 148-commit budget and the `v1.0.0` coverage-tag gate. The work is mechanical and the contracts are already in `todo.md`.
