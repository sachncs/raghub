"""Session memory adapters.

The package is currently a placeholder for future session-memory
backends (e.g. sliding-window Redis stores). Callers that need
custom memory today should reach into
:mod:`raghub.conversation.manager` and
:mod:`raghub.conversation.sliding_window` directly.
"""