"""Cache abstractions.

The package is currently a placeholder for future cache adapters
(in-memory LRU, Redis, etc.). Callers that need caching today should
add their own thin wrapper rather than depending on this package.
"""